name: neeble

dependencies:
  neeble:
    repository: https://github.com/KevinCaires/neeble
    manifest: okteto.yml
    branch: develop

build:
  neeble:
    context: .
    dockerfile: Dockerfile

deploy:
  compose:
    file: docker-compose.yml
    services:
      - mysql
      - neeble
