name: neeble

dependencies:
  neeble:
    repository: https://github.com/KevinCaires/neeble
    manifest: okteto.yml
    branch: develop

build:
  neeble:
    context: .
    dockerfile: Dockerfile
    target: deploy

deploy:
  compose:
    file: docker-compose.yml
    services:
      - mysql
    file: docker-compose.yml
    services:
      - neeble
